# Database Dockerfile for MongoDB
# Uses official MongoDB image with custom initialization

FROM mongo:5.0

# Set environment variables
ENV MONGO_INITDB_DATABASE=todo_list

# Expose MongoDB default port
EXPOSE 27017

# Create a volume for persistent data
VOLUME /data/db

# Set up MongoDB with default configuration
# The container will automatically create the todo_list database
CMD ["mongod", "--bind_ip_all"]
